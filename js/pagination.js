(function(){const perPage=6;let page=1;function render(){const grid=document.querySelector('#video-grid'),pager=document.querySelector('#pagination');if(!grid)return;const total=Math.ceil(videoProjects.length/perPage),items=videoProjects.slice((page-1)*perPage,page*perPage);grid.innerHTML=items.map(v=>`<article class="project-card"><div class="video-frame"><iframe loading="lazy" src="https://www.youtube.com/embed/${v.video}" title="${v.title}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></div><div class="project-meta mt-5"><div class="flex items-start justify-between gap-4"><h3 class="project-title">${v.title}</h3><span class="text-xs text-zinc-400">0${(page-1)*perPage+items.indexOf(v)+1}</span></div><p class="project-description mt-2">${v.description}</p><div class="mt-4 flex flex-wrap gap-x-4 gap-y-2">${v.skills.map(s=>`<span class="skill-tag">${s}</span>`).join('')}</div></div></article>`).join('');pager.innerHTML=`<button class="pager-button" data-page="${page-1}" ${page===1?'disabled':''}>←</button>${Array.from({length:total},(_,i)=>`<button class="pager-button ${page===i+1?'active':''}" data-page="${i+1}">${String(i+1).padStart(2,'0')}</button>`).join('')}<button class="pager-button" data-page="${page+1}" ${page===total?'disabled':''}>→</button>`;pager.querySelectorAll('button').forEach(btn=>btn.addEventListener('click',()=>{const next=Number(btn.dataset.page);if(next>0&&next<=total){page=next;render();document.querySelector('#work').scrollIntoView({behavior:'smooth'});}}))}document.addEventListener('DOMContentLoaded',render)})();
